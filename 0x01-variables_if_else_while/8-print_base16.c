#include <stdio.h>

/**
 * main - print the numbers in base 16
 * Return: Always 0
 */
int main(void)
{
	int num;
	chat la;

	for (num = 0; num < 10; numer++)
	putchar((num % 10) + '0');

	for (la = 'a'; la <= 'f'; la++)
	putchar(la);

	putchar('\n');

	return (0);
}

